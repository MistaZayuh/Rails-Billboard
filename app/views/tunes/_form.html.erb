<%= form_with(model: [@band, @tune], local: true, class: "ui form") do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <br />
  <%= f.label :genre %>
  <%= f.text_field :genre, required: true %>
  <br />
  <%= f.label :billboard_id %>
  <%= f.collection_select :billboard_id, Billboard.order(:name), :id, :name, include_blank: true %>
  <%# I have trieds for hours to update the @tune billboard_id using a form. Nothing I've found on google has worked %>
  <br />
  <%= f.submit class: "ui purple inverted button" %>
  <% @tune.billboard_id = params[:billboard_id]%>
<% end %>